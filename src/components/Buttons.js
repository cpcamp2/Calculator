import React from 'react';
import Button from './Button';

const Buttons = (props) => (
  <div>
    <Button />
    <Button />
    <Button />
    <Button />
    <Button />
    <Button />
  </div>
);

export default Buttons;
